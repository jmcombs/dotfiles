# Global git ignore (core.excludesFile default: ~/.config/git/ignore)

# macOS Finder metadata
.DS_Store

# Editor / tool local settings
**/.claude/settings.local.json

# pi agent-harness output — never belongs in any project repo
.pi-subagents/
.pi/

**/.claude/.cc-writes/
