# PLAN
## Phase 1 — interval-merge
### Actionable TODOs
- [ ] Implement the function(s) in `src/interval-merge.ts` per the test contract.
### Testing Gates
| Criterion | Command | Expected |
|---|---|---|
| interval-merge | `node --test test/interval-merge.test.ts` | exit 0; all tests pass |
