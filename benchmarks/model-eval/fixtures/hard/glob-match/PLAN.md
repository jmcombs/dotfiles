# PLAN
## Phase 1 — glob-match
### Actionable TODOs
- [ ] Implement the function(s) in `src/glob-match.ts` per the test contract.
### Testing Gates
| Criterion | Command | Expected |
|---|---|---|
| glob-match | `node --test test/glob-match.test.ts` | exit 0; all tests pass |
