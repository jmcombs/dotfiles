# Phase 1 — Ten-pin bowling scorer
### Actionable TODOs
- [ ] Implement `class BowlingGame` in `src/bowling.ts`: `roll(pins)` and `score()` for a full game (spares, strikes, 10th-frame bonus rolls).
### Testing Gates
| Criterion | Command | Expected |
|---|---|---|
| bowling | `node --test test/bowling.test.ts` | exit 0; all pass |
