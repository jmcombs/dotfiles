# Phase 1 — Roman numerals
### Actionable TODOs
- [ ] Implement `toRoman(n: number): string` and `fromRoman(s: string): number` in `src/roman.ts` (1..3999).
### Testing Gates
| Criterion | Command | Expected |
|---|---|---|
| roman | `node --test test/roman.test.ts` | exit 0; all pass |
